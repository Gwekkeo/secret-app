<!DOCTYPE html>
<html>
<head>
	<title>HOME</title>
	<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css"/>
</head>
<body>
	  <header>
	      <ul class="nav nav-pills nav-fill">
	        <li class="nav-item"> <a class="nav-link active" href="<%= root_path%>" >Accueil</a> </li>
	        <li class="nav-item"> <a class="nav-link" href="/login" >Connexion</a> </li>
	        <li class="nav-item"> <a class="nav-link" href="<%= users_path %>" >Inscription</a> </li>
	        <% if logged_in? %>
	        <li class="nav-item" >
				<%= link_to "Log out", logout_path, method: :delete, class: 'nav-link' %>
			</li>
	        <% else %>
	        	<li class="nav-item" >
	        		 <%= link_to "Log in", "/login", class: 'nav-link'%> 
	        	</li>
	    	<% end %>
		</ul>
	</header>
<!-- users = possède GET & POST -->

	<div class="center jumbotron">
	  <h1>Bienvenue !</h1>
	  <h3> Et si on inscrivait des étudiants ? 🎓 </h3>
	  <%= link_to "New Student", users_path, class: "btn btn-lg btn-primary" %>

	  <% if logged_in? %>
	   <%= link_to "GO Page Secrete", "/secret-page", class: "btn btn-lg btn-warning" %>
	  <% else %>
	   <%= link_to "GO Page Secrete", "/login", class: "btn btn-lg btn-warning" %>
	  <% end %>

	</div>

	<h2>TOUS LES EMAILS:</h2>
	<ul>
		<% @users_tab.each do |user| %>
		<li> <a href="<%= user_path(user.id) %>"> <%= user.email %> </a> </li>
		<% end %>
	</ul>

</body>
</html>